{
  "name": "BlinkLane Review",
  "install": "sudo apt-get update && sudo apt-get install -y --no-install-recommends python3.12-venv ffmpeg && python3 -m venv .venv && . .venv/bin/activate && python -m pip install --upgrade pip && python -m pip install -e '.[dev,yolo]' && npm install",
  "terminals": [
    {
      "name": "backend",
      "command": ". .venv/bin/activate && uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000"
    },
    {
      "name": "frontend",
      "command": "npm run dev"
    }
  ],
  "ports": [8000, 5173]
}
